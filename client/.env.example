# Frontend build-time config. Copy to .env.local for local overrides.

# Backend origin used by production builds. Leave EMPTY for local dev
# (requests hit /api and go through the Vite dev proxy).
# Example: VITE_API_BASE=https://your-space.hf.space
VITE_API_BASE=
